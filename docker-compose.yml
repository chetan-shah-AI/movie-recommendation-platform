version: "3.9"

services:
  recommender-api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: movie-recommender-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./logs:/app/logs

    restart: unless-stopped